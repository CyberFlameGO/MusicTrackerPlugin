name: music-tracker
version: '${project.version}'
main: org.commandomc.spotifyplugin.SpotifyPlugin
api-version: 1.19

commands:
  auth:
    description: authenticate with spotify to display your status
    usage: /auth
  spotifySync:
    description: Refresh everyone's spotify song
    usage: /spotifySync